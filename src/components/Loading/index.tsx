import React from "react";
import * as Styled from "./styles";

const Loading = () => {
  return (
    <Styled.LoadingContainer>
      <Styled.Loader />
    </Styled.LoadingContainer>
  );
};

export default Loading;
